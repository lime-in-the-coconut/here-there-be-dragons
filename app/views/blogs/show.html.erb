<html>
<p id="notice"><%= notice %></p>

<div style= "margin-top:30px">
	<% @blog.tags.each do |tag| %>

	<span style ="background-color#:afcbaa;padding:2px;margin:4px"><%= tag.tag %></span>
	<% end %>
</div>
<div style="margin left:5px">
	<p>
  <strong>Title:</strong>
  	<%= @blog.title %>
	</p>

	<p>
  	<strong>Content:</strong>
  	<%= @blog.content %>
	</p>
</div>
<div style="padding:20px;background-color:#afcbaa">
	<% @comments.each do |comment|%>
	<%= comment.name %> - <%= comment.content %><br/>
	<% end%>
</div>
</html>

<%= link_to 'Add a comment', new_comment_path(:blog_id => @blog.id) %>

<%= link_to 'Edit', edit_blog_path(@blog) %> |
<%= link_to 'Back', blogs_path %>

